import { UsernameTakenDirective } from './username-taken.directive';

describe('UsernameTakenDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameTakenDirective();
    expect(directive).toBeTruthy();
  });
});
