import { EmailTakenDirective } from './email-taken.directive';

describe('EmailTakenDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailTakenDirective();
    expect(directive).toBeTruthy();
  });
});
