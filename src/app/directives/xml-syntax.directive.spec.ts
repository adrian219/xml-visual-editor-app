import { XmlSyntaxDirective } from './xml-syntax.directive';

describe('XmlSyntaxDirective', () => {
  it('should create an instance', () => {
    const directive = new XmlSyntaxDirective();
    expect(directive).toBeTruthy();
  });
});
